// Backward-compatible re-export for any existing imports.
// Keep this file empty of non-component exports to avoid react-refresh complaints.

import AuthProvider from "./AuthProvider";

export default AuthProvider;
export { AuthProvider };

